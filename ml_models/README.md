# Model Artifacts

- `xgboost_risk_model.joblib` is the preferred composite risk model generated by `scripts/train_models.py`.
- `model.pkl` and `paysim_model.pkl` are legacy artifacts from the earlier single-pipeline version and can still be retained for reference.
